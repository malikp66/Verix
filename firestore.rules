rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All Firestore operations now go through Firebase Admin SDK (server-side).
    // Client-side direct access is blocked.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
